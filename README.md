# benchmark-tool-valispace

## To setup the db use:
```
    flask --app <your app name> db init
    flask --app <your app name> db migrate -m "Example migrate message"
    flask --app <your app name> db upgrade
```
